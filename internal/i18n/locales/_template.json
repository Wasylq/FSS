{
  "Additional Commands:": "Additional Commands:",
  "Additional help topics:": "Additional help topics:",
  "Aliases:": "Aliases:",
  "Available Commands:": "Available Commands:",
  "Check environment health: config, database, Stash, tools": "Check environment health: config, database, Stash, tools",
  "Check whether a URL is supported and show the matching scraper": "Check whether a URL is supported and show the matching scraper",
  "Compare a creator's catalogue across the storefronts they sell it on": "Compare a creator's catalogue across the storefronts they sell it on",
  "Compare a creator's catalogue across the storefronts they sell it on.\n\nCreators list the same clips on several sites at different prices, and each site\nalso carries some the others do not. For every creator this reports what each\nstore holds, which titles are on more than one, where each shared title is\ncheapest, and what only one store has.\n\nStorefronts are grouped using creators.d. Without any creator files, the studios\nare clustered by the same heuristic as `fss creators suggest`, and the report\nsays so — define the creators to make the grouping exact.": "Compare a creator's catalogue across the storefronts they sell it on.\n\nCreators list the same clips on several sites at different prices, and each site\nalso carries some the others do not. For every creator this reports what each\nstore holds, which titles are on more than one, where each shared title is\ncheapest, and what only one store has.\n\nStorefronts are grouped using creators.d. Without any creator files, the studios\nare clustered by the same heuristic as `fss creators suggest`, and the report\nsays so — define the creators to make the grouping exact.",
  "Create a default config file": "Create a default config file",
  "Error:": "Error:",
  "Examples:": "Examples:",
  "Export studios from the SQLite store as JSON and/or CSV files, one per studio,\nnamed the same way the flat store names them.\n\nWith no arguments every tracked studio is exported.": "Export studios from the SQLite store as JSON and/or CSV files, one per studio,\nnamed the same way the flat store names them.\n\nWith no arguments every tracked studio is exported.",
  "Fetch a URL and detect which platform/CMS it uses": "Fetch a URL and detect which platform/CMS it uses",
  "Fetches the given URL once and checks the response for known platform\nsignals (Aylo instance_token, Algolia API, psmcdn.net, ModelCentro, etc.).\n\nReports the detected platform and the corresponding util package, or\n\"unknown\" if no signal matches. Useful when deciding whether a new site\nneeds a standalone scraper or belongs to an existing shared package.": "Fetches the given URL once and checks the response for known platform\nsignals (Aylo instance_token, Algolia API, psmcdn.net, ModelCentro, etc.).\n\nReports the detected platform and the corresponding util package, or\n\"unknown\" if no signal matches. Useful when deciding whether a new site\nneeds a standalone scraper or belongs to an existing shared package.",
  "Flags:": "Flags:",
  "FullStudioScraper — scrape all scenes and metadata from a studio URL": "FullStudioScraper — scrape all scenes and metadata from a studio URL",
  "Generate shell completion scripts": "Generate shell completion scripts",
  "Generate shell completion scripts for your shell.\n\nTo load completions:\n\n  bash:\n    source \u003c(fss completion bash)\n\n  zsh:\n    fss completion zsh \u003e \"${fpath[1]}/_fss\"\n\n  fish:\n    fss completion fish | source\n\n  powershell:\n    fss completion powershell | Out-String | Invoke-Expression": "Generate shell completion scripts for your shell.\n\nTo load completions:\n\n  bash:\n    source \u003c(fss completion bash)\n\n  zsh:\n    fss completion zsh \u003e \"${fpath[1]}/_fss\"\n\n  fish:\n    fss completion fish | source\n\n  powershell:\n    fss completion powershell | Out-String | Invoke-Expression",
  "Global Flags:": "Global Flags:",
  "Help about any command": "Help about any command",
  "Help provides help for any command in the application.\nSimply type %s help [path to command] for full details.": "Help provides help for any command in the application.\nSimply type %s help [path to command] for full details.",
  "Interact with a Stash instance": "Interact with a Stash instance",
  "List Stash scenes with no StashDB metadata": "List Stash scenes with no StashDB metadata",
  "List all registered scrapers and their URL patterns": "List all registered scrapers and their URL patterns",
  "List all studios tracked in the SQLite database": "List all studios tracked in the SQLite database",
  "List the creators defined in creators.d": "List the creators defined in creators.d",
  "List the creators defined in creators.d.\n\nA creator binds the several storefronts one person sells the same catalogue on,\nso a single command can scrape all of them:\n\n    fss scrape --creator \"Mara Vance\"\n    fss scrape --all-creators --stale 7d\n\nDefinitions live in one YAML file per creator (default ~/.config/fss/creators.d).\nThey carry no credentials, so a directory of them can be kept in git and shared;\npoint creators_dir at a clone to use someone else's set.": "List the creators defined in creators.d.\n\nA creator binds the several storefronts one person sells the same catalogue on,\nso a single command can scrape all of them:\n\n    fss scrape --creator \"Mara Vance\"\n    fss scrape --all-creators --stale 7d\n\nDefinitions live in one YAML file per creator (default ~/.config/fss/creators.d).\nThey carry no credentials, so a directory of them can be kept in git and shared;\npoint creators_dir at a clone to use someone else's set.",
  "Load one or more studio JSON files (or every *.json in a directory) into the\nSQLite store.\n\nThe database schema is a superset of the JSON layout, so nothing is lost. Each\nfile's own \"studioUrl\" decides which studio it belongs to — filenames are not\nparsed.\n\nExisting database scenes are merged with the file by default: a scene present in\nboth takes the file's values, price history is carried forward, and scenes only\nin the database are kept. Pass --replace to make the file authoritative instead,\nwhich deletes database scenes absent from it.": "Load one or more studio JSON files (or every *.json in a directory) into the\nSQLite store.\n\nThe database schema is a superset of the JSON layout, so nothing is lost. Each\nfile's own \"studioUrl\" decides which studio it belongs to — filenames are not\nparsed.\n\nExisting database scenes are merged with the file by default: a scene present in\nboth takes the file's values, price history is carried forward, and scenes only\nin the database are kept. Pass --replace to make the file authoritative instead,\nwhich deletes database scenes absent from it.",
  "Load studio JSON files into the SQLite database": "Load studio JSON files into the SQLite database",
  "Manage FSS configuration": "Manage FSS configuration",
  "Match FSS JSON scenes against Stash and push metadata": "Match FSS JSON scenes against Stash and push metadata",
  "Match FSS JSON scenes against Stash scenes by filename, then push metadata.\n\nDefault is dry-run — shows what would change. Pass --apply to actually write to Stash.": "Match FSS JSON scenes against Stash scenes by filename, then push metadata.\n\nDefault is dry-run — shows what would change. Pass --apply to actually write to Stash.",
  "Match video files against FSS metadata and write NFO sidecar files": "Match video files against FSS metadata and write NFO sidecar files",
  "Print the config file path": "Print the config file path",
  "Print version and check for updates": "Print version and check for updates",
  "Propose creators.d files by clustering the studios you already scrape": "Propose creators.d files by clustering the studios you already scrape",
  "Propose creators.d files by clustering the studios you already scrape.\n\nStudios are grouped when their names overlap (VeraQuill / Vera Quill Films) or\nwhen they share a dominant performer (Duchess Nyx / Nyx Vale). Both are\nheuristics, so the proposals print to stdout for review; --write commits them.": "Propose creators.d files by clustering the studios you already scrape.\n\nStudios are grouped when their names overlap (VeraQuill / Vera Quill Films) or\nwhen they share a dominant performer (Duchess Nyx / Nyx Vale). Both are\nheuristics, so the proposals print to stdout for review; --write commits them.",
  "Scrape all scenes from one or more studio URLs, or from a creator's storefronts": "Scrape all scenes from one or more studio URLs, or from a creator's storefronts",
  "Scrape all scenes from one or more studio URLs.\n\nStudios can be named directly, or selected through creators.d — one YAML file\nper creator listing the several storefronts one person sells on:\n\n    fss scrape https://clipmarket.example/studio/4021/mara-vance\n    fss scrape --creator \"Mara Vance\"\n    fss scrape --all-creators --stale 7d": "Scrape all scenes from one or more studio URLs.\n\nStudios can be named directly, or selected through creators.d — one YAML file\nper creator listing the several storefronts one person sells on:\n\n    fss scrape https://clipmarket.example/studio/4021/mara-vance\n    fss scrape --creator \"Mara Vance\"\n    fss scrape --all-creators --stale 7d",
  "Stash API key (env: FSS_STASH_API_KEY)": "Stash API key (env: FSS_STASH_API_KEY)",
  "Stash server URL (default from config)": "Stash server URL (default from config)",
  "Undo a prior --include-stashbox import using the changelog": "Undo a prior --include-stashbox import using the changelog",
  "Undo changes recorded in fss-stashbox-changelog.json for one or more scenes.\n\nReverts: title, date, urls (the added ones), tags (the added ones), performers\n(the added ones).\n\nNOT reverted (limitations of what the changelog stores):\n  - details: only a 60-char preview is recorded; can't restore the original.\n  - cover:   the original cover URL was never recorded.\n\nDefault is dry-run — pass --apply to actually write changes.": "Undo changes recorded in fss-stashbox-changelog.json for one or more scenes.\n\nReverts: title, date, urls (the added ones), tags (the added ones), performers\n(the added ones).\n\nNOT reverted (limitations of what the changelog stores):\n  - details: only a 60-char preview is recorded; can't restore the original.\n  - cover:   the original cover URL was never recorded.\n\nDefault is dry-run — pass --apply to actually write changes.",
  "Usage:": "Usage:",
  "Use \"%s [command] --help\" for more information about a command.": "Use \"%s [command] --help\" for more information about a command.",
  "Walk a directory of video files, match them against FSS scene metadata,\nand write .nfo sidecar files alongside each matched video.\n\nThe .nfo files can be picked up by Stash via the community NFO scraper.\n\nDefault is dry-run — shows what would be written. Pass --apply to write.": "Walk a directory of video files, match them against FSS scene metadata,\nand write .nfo sidecar files alongside each matched video.\n\nThe .nfo files can be picked up by Stash via the community NFO scraper.\n\nDefault is dry-run — shows what would be written. Pass --apply to write.",
  "Write studio JSON/CSV files out of the SQLite database": "Write studio JSON/CSV files out of the SQLite database",
  "actually write .nfo files (default is dry-run)": "actually write .nfo files (default is dry-run)",
  "actually write changes (default is dry-run)": "actually write changes (default is dry-run)",
  "add resolution tags (4K/FHD/HD Available)": "add resolution tags (4K/FHD/HD Available)",
  "allow a 0-scene or collapsed-coverage --full/--refresh to overwrite a populated studio": "allow a 0-scene or collapsed-coverage --full/--refresh to overwrite a populated studio",
  "allow cover URLs that resolve to private/loopback IPs (for local media servers); disabled by default to prevent SSRF when importing third-party JSON": "allow cover URLs that resolve to private/loopback IPs (for local media servers); disabled by default to prevent SSRF when importing third-party JSON",
  "allow poster URLs that resolve to private/loopback IPs (for local media servers); disabled by default to prevent SSRF": "allow poster URLs that resolve to private/loopback IPs (for local media servers); disabled by default to prevent SSRF",
  "also process scenes that have StashDB data": "also process scenes that have StashDB data",
  "also propose creators whose catalogue is on a single storefront": "also propose creators whose catalogue is on a single storefront",
  "also write every shared title to this CSV file": "also write every shared title to this CSV file",
  "delete performer/tag/category rows no scene references any more": "delete performer/tag/category rows no scene references any more",
  "directory containing FSS JSON files (default: config out_dir)": "directory containing FSS JSON files (default: config out_dir)",
  "directory containing fss-stashbox-changelog.json (default: config out_dir)": "directory containing fss-stashbox-changelog.json (default: config out_dir)",
  "directory of creator YAML files (default: config creators_dir, else ~/.config/fss/creators.d)": "directory of creator YAML files (default: config creators_dir, else ~/.config/fss/creators.d)",
  "do not write fss-report.txt": "do not write fss-report.txt",
  "download each scene's thumbnail to a -poster file beside the video and reference it from the .nfo (off by default: one network fetch per scene)": "download each scene's thumbnail to a -poster file beside the video and reference it from the .nfo (off by default: one network fetch per scene)",
  "enable SQLite store (no value = the configured db, or ~/.local/share/fss/fss.db; or pass a path)": "enable SQLite store (no value = the configured db, or ~/.local/share/fss/fss.db; or pass a path)",
  "export formats: json, csv, or json,csv (default from config)": "export formats: json, csv, or json,csv (default from config)",
  "filter Stash scenes by performer name": "filter Stash scenes by performer name",
  "filter Stash scenes by studio name": "filter Stash scenes by studio name",
  "filter Stash scenes by substring in file path": "filter Stash scenes by substring in file path",
  "filter by performer name": "filter by performer name",
  "filter by studio name": "filter by studio name",
  "filter by substring in file path": "filter by substring in file path",
  "help for %s": "help for %s",
  "help for this command": "help for this command",
  "help language code, e.g. ko (see docs/translations.md)": "help language code, e.g. ko (see docs/translations.md)",
  "how many of the widest price spreads to list per creator": "how many of the widest price spreads to list per creator",
  "human-readable label for this studio (stored when --db is set)": "human-readable label for this studio (stored when --db is set)",
  "ignore existing data, scrape everything from scratch": "ignore existing data, scrape everything from scratch",
  "import marker tag (default from config)": "import marker tag (default from config)",
  "increase debug verbosity (repeat for more: -d, -dd, -ddd)": "increase debug verbosity (repeat for more: -d, -dd, -ddd)",
  "let a re-scrape blank fields it no longer returns (default: keep the stored value)": "let a re-scrape blank fields it no longer returns (default: keep the stored value)",
  "limit number of Stash scenes to process (0 = all)": "limit number of Stash scenes to process (0 = all)",
  "limit number of results (0 = all)": "limit number of results (0 = all)",
  "list the titles only one storefront carries": "list the titles only one storefront carries",
  "load scenes from the SQLite store instead of JSON (no value = the configured db, or the default location)": "load scenes from the SQLite store instead of JSON (no value = the configured db, or the default location)",
  "make each file authoritative: delete stored scenes it does not contain": "make each file authoritative: delete stored scenes it does not contain",
  "max parallel fetchers (0 = use config/default)": "max parallel fetchers (0 = use config/default)",
  "milliseconds between page requests (default 500 from config; 0 = no delay)": "milliseconds between page requests (default 500 from config; 0 = no delay)",
  "only revert these fields (title,date,urls,tags,performers); default: all revertible": "only revert these fields (title,date,urls,tags,performers); default: all revertible",
  "only scrape studios not scraped within this window, e.g. 12h, 7d, 2w (needs --db)": "only scrape studios not scraped within this window, e.g. 12h, 7d, 2w (needs --db)",
  "only update these fields (title,details,date,urls,tags,performers,studio,cover); default: all": "only update these fields (title,details,date,urls,tags,performers,studio,cover); default: all",
  "only use scenes featuring these performers (repeatable: any match)": "only use scenes featuring these performers (repeatable: any match)",
  "only use scenes from these studios — a studio URL, a studio name, or a per-scene studio/sub-brand name (repeatable: any match)": "only use scenes from these studios — a studio URL, a studio name, or a per-scene studio/sub-brand name (repeatable: any match)",
  "only use scenes from this creator's storefronts, as defined in creators.d (repeatable: any match)": "only use scenes from this creator's storefronts, as defined in creators.d (repeatable: any match)",
  "output a markdown table": "output a markdown table",
  "output directory (default from config)": "output directory (default from config)",
  "overwrite existing .nfo files": "overwrite existing .nfo files",
  "path to SQLite database (no value = default location)": "path to SQLite database (no value = default location)",
  "path to SQLite database (no value = the configured db, or the default location)": "path to SQLite database (no value = the configured db, or the default location)",
  "per-scraper delay override, e.g. --site-delay manyvids=0,pornhub=2000 (overrides --delay for matching sites)": "per-scraper delay override, e.g. --site-delay manyvids=0,pornhub=2000 (overrides --delay for matching sites)",
  "re-fetch metadata for all known scenes, soft-delete missing": "re-fetch metadata for all known scenes, soft-delete missing",
  "replace the performers on every scene this run scrapes (repeat, or comma-separate, for several)": "replace the performers on every scene this run scrapes (repeat, or comma-separate, for several)",
  "replace the studio on every scene this run scrapes": "replace the studio on every scene this run scrapes",
  "report what would be imported without writing": "report what would be imported without writing",
  "revert every changelog entry for the scene (default: only the most recent)": "revert every changelog entry for the scene (default: only the most recent)",
  "scrape every storefront defined for this creator in creators.d (repeatable)": "scrape every storefront defined for this creator in creators.d (repeatable)",
  "scrape every storefront of every creator in creators.d": "scrape every storefront of every creator in creators.d",
  "set cover image from FSS thumbnail (also implicitly enabled when 'cover' is in --fields)": "set cover image from FSS thumbnail (also implicitly enabled when 'cover' is in --fields)",
  "set organized flag on imported scenes": "set organized flag on imported scenes",
  "specific JSON files to load": "specific JSON files to load",
  "tag for stashbox overrides (default from config)": "tag for stashbox overrides (default from config)",
  "version for %s": "version for %s",
  "version for this command": "version for this command",
  "with --write, overwrite creator files that already exist": "with --write, overwrite creator files that already exist",
  "write the proposals into the creators directory instead of printing them": "write the proposals into the creators directory instead of printing them"
}
